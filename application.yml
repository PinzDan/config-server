spring:
  datasource:
    url: jdbc:mysql://localhost:3306/course
    username: root
    password: ""   # oppure rimuovi la riga se non usi password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
