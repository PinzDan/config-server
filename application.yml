spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/course
    username: root
    password: 

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
